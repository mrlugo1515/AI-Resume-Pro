{
  "name": "ai-resume-pro",
  "version": "0.1.0",
  "private": true,
  "description": "AI Resume Pro - Automatically tailor resumes and cover letters to job descriptions",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "setup": "npm install && npm run setup --prefix frontend && npm run setup --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
